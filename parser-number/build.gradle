apply plugin: 'java-library'

description = 'parser-number'

dependencies {
    api(
            project(':parser-api'),
            'org.slf4j:slf4j-api'
    )

    testImplementation(
            'ch.qos.logback:logback-classic',
            'org.junit.jupiter:junit-jupiter',
            'org.assertj:assertj-core'
    )
}